/* Reverse method is an action for an Array, .reverse(), reverses the items in the Array */

let myArray = ["john","matthew","luke", "mark"];
console.log ("im the array", myArray.reverse());

let reversedArray = myArray.reverse();
console.log ("im the reversed Array", reversedArray);